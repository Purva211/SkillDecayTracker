**Key files:**

- `app.py` — Main application logic and API endpoints
- `database.py` — Logic for connecting and interacting with skill/user data
- `skill_data.json` — Stored skill information
- `users.json` — User profiles and tracking data
- `requirements.txt` — Python package dependencies

🚀 Features

✅ Load and manage skill data  
✅ Store users and associated skill performance  
✅ Track skill usage frequency  
✅ Identify decreasing skill interaction over time  
✅ Ready for expansion with ML or dashboard UI

🧪 Requirements

This project uses **Python**, so you’ll need:

- Python 3.8+
- Pip

📥 Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/Purva211/SkillDecayTracker.git
   cd SkillDecayTracker
   ```
